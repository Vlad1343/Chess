.venv/
__pycache__/
.pytest_cache/
*.pyc
dist/
*.npz

# Engine tuning parameters must ship in the submission zip. A global gitignore
# often excludes config.json (usually a secrets file); ours has none.
!config.json
